import React from "react";
import reactDOM from "react-dom";

const root = reactDOM.createRoot(document.getElementById("root"));

const HeadingComponent = () => (
  <>
    <h1>Hello I am Javeed</h1>
  </>
);

root.render(<HeadingComponent />);
